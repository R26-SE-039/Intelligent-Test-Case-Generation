[pytest]
testpaths = tests
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
markers =
    unit: fast, pure-logic tests with no external dependencies
    integration: exercises the FastAPI app via TestClient (no DB/network)
